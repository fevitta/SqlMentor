name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint (ruff check)
        run: ruff check src/ tests/

      - name: Format check (ruff format)
        run: ruff format --check src/ tests/

      - name: Type check (mypy)
        run: mypy src/sqlmentor/

      - name: Run tests with coverage
        run: pytest tests/ -v --tb=short --cov=sqlmentor --cov-fail-under=90

  check-oracle-paths:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.filter.outputs.oracle }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            oracle:
              - 'src/sqlmentor/collector.py'
              - 'src/sqlmentor/queries/**'
              - 'tests/integration/**'

  integration-oracle:
    runs-on: ubuntu-latest
    needs: [lint-and-test, check-oracle-paths]
    if: >-
      needs.check-oracle-paths.outputs.should_run == 'true'
      || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - name: Start Oracle XE 21c container
        run: |
          docker run -d \
            --name sqlmentor-oracle \
            -p 1521:1521 \
            -e ORACLE_PASSWORD=SysPwd123 \
            -v ${{ github.workspace }}/tests/integration/initdb:/container-entrypoint-initdb.d \
            --health-cmd "healthcheck.sh" \
            --health-interval 10s \
            --health-timeout 5s \
            --health-retries 30 \
            --health-start-period 120s \
            gvenzl/oracle-xe:21-slim

      - name: Wait for Oracle to be healthy
        run: |
          echo "Waiting for Oracle..."
          for i in $(seq 1 60); do
            STATUS=$(docker inspect --format='{{.State.Health.Status}}' sqlmentor-oracle 2>/dev/null || echo "starting")
            if [ "$STATUS" = "healthy" ]; then
              echo "Oracle is healthy!"
              break
            fi
            echo "Attempt $i/60 - status: $STATUS"
            sleep 5
          done
          STATUS=$(docker inspect --format='{{.State.Health.Status}}' sqlmentor-oracle)
          if [ "$STATUS" != "healthy" ]; then
            echo "Oracle failed to start. Container logs:"
            docker logs sqlmentor-oracle --tail 50
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run Oracle integration tests
        env:
          SQLMENTOR_TEST_HOST: localhost
          SQLMENTOR_TEST_PORT: "1521"
          SQLMENTOR_TEST_SERVICE: XEPDB1
          SQLMENTOR_TEST_USER: SQLMENTOR_TEST
          SQLMENTOR_TEST_PASSWORD: TestPwd123
          SQLMENTOR_TEST_SCHEMA: SQLMENTOR_TEST
        run: pytest tests/integration/ -v --tb=short --ignore=tests/integration/mariadb

      - name: Stop Oracle container
        if: always()
        run: docker rm -f sqlmentor-oracle || true

  check-mariadb-paths:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.filter.outputs.mariadb }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            mariadb:
              - 'src/sqlmentor/adapters/mariadb.py'
              - 'src/sqlmentor/collector.py'
              - 'src/sqlmentor/report.py'
              - 'tests/integration/mariadb/**'

  integration-mariadb:
    runs-on: ubuntu-latest
    needs: [lint-and-test, check-mariadb-paths]
    if: >-
      needs.check-mariadb-paths.outputs.should_run == 'true'
      || github.event_name == 'workflow_dispatch'
    steps:
      - uses: actions/checkout@v4

      - name: Start MariaDB 10.6 container
        run: |
          docker run -d \
            --name sqlmentor-mariadb \
            -p 3307:3306 \
            -e MARIADB_ROOT_PASSWORD=RootPwd123 \
            -v ${{ github.workspace }}/tests/integration/mariadb/initdb:/docker-entrypoint-initdb.d \
            --health-cmd "mysqladmin ping -h localhost --user=root --password=RootPwd123" \
            --health-interval 5s \
            --health-timeout 5s \
            --health-retries 20 \
            --health-start-period 30s \
            mariadb:10.6 \
            --lower-case-table-names=0 \
            --performance-schema=ON \
            --performance-schema-consumer-events-statements-current=ON \
            --performance-schema-consumer-events-statements-history=ON \
            --performance-schema-consumer-events-waits-current=ON

      - name: Wait for MariaDB to be healthy
        run: |
          echo "Waiting for MariaDB..."
          for i in $(seq 1 40); do
            STATUS=$(docker inspect --format='{{.State.Health.Status}}' sqlmentor-mariadb 2>/dev/null || echo "starting")
            if [ "$STATUS" = "healthy" ]; then
              echo "MariaDB is healthy!"
              break
            fi
            echo "Attempt $i/40 - status: $STATUS"
            sleep 5
          done
          STATUS=$(docker inspect --format='{{.State.Health.Status}}' sqlmentor-mariadb)
          if [ "$STATUS" != "healthy" ]; then
            echo "MariaDB failed to start. Container logs:"
            docker logs sqlmentor-mariadb --tail 50
            exit 1
          fi

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev,mariadb]"

      - name: Run MariaDB integration tests
        env:
          SQLMENTOR_MARIADB_HOST: localhost
          SQLMENTOR_MARIADB_PORT: "3307"
          SQLMENTOR_MARIADB_USER: sqlmentor_test
          SQLMENTOR_MARIADB_PASSWORD: TestPwd123
          SQLMENTOR_MARIADB_DATABASE: SQLMENTOR_TEST
          SQLMENTOR_MARIADB_SCHEMA: SQLMENTOR_TEST
        run: pytest tests/integration/mariadb/ -v --tb=short

      - name: Stop MariaDB container
        if: always()
        run: docker rm -f sqlmentor-mariadb || true
